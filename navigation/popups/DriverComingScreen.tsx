import React, { useEffect, useRef, useState } from 'react';

import { View, Text, StyleSheet, TextInput, Image, TouchableOpacity, Modal, FlatList, Alert, ScrollView, ActivityIndicator } from 'react-native';
const DriverComingScreen = () => {
    return(
        <View>
            <Text>Hello</Text>
        </View>
    )
}

export default DriverComingScreen